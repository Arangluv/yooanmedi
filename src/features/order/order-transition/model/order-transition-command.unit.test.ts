import { describe, it, expect, vi } from 'vitest';
import { OrderTransitionCommand } from './order-transition-command';

describe('OrderTransitionCommand', () => {
  // it('execute 성공 시 주문상태 변경과 관련된 모든 액션이 실행된다', async () => {
  //   const result = await orderTransitionCommand.execute();
  //   expect(() => orderTransitionCommand.execute()).not.toThrow();
  // });
  // it('execute 실패 시 에러를 throw한다.', async () => {
  //   vi.spyOn(orderTransitionCommand, 'execute').mockRejectedValue(() => {
  //     throw new Error('execute fail');
  //   });
  //   await expect(() => orderTransitionCommand.execute()).rejects.toThrowError();
  // });
});
